[project]
name = "churn-prediction-api"
version = "1.0.0"
description = "Serves a churn model as a REST API, with no ML runtime on board"
requires-python = ">=3.9"
dependencies = [
    "fastapi>=0.110",
    "pydantic>=2.0",
]

[tool.pytest.ini_options]
addopts = "-q"
